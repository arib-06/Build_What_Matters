# Optional: add a Gemini API key for live help in the local prototype.
# Never commit a real key. For production, call Gemini from a server-side
# proxy so the key is not exposed in the browser bundle.
VITE_GEMINI_API_KEY=
